{
	"name": "bookmyride",
	"version": "1.0.0",
	"description": "- It is a Rent a car website where Customers can book the cars by paying through Stripe.  - Admin can see the bookings and can manage the cars.  - If a car is booked for a time span no other customer can book the same car on same time span.",
	"main": "index.js",
	"scripts": {
		"client": "cd client && npm start",
		"server": "cd server && npm start",
		"start": "concurrently  \"npm run server\" \"npm run client\"",
		"build": "concurrently  \"cd client && npm run build\"",
		"install": "concurrently  \"cd client && npm install\"  \"cd server && npm install\"",
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"repository": {
		"type": "git",
		"url": "git+https://github.com/neeravgg/BookMyRide.git"
	},
	"author": "",
	"license": "ISC",
	"bugs": {
		"url": "https://github.com/neeravgg/BookMyRide/issues"
	},
	"homepage": "https://github.com/neeravgg/BookMyRide#readme",
	"dependencies": {
		"concurrently": "^8.2.1"
	}
}
